## Agent instructions

When working in `Tools/px4_lockstep_julia`:

1) Read `Tools/px4_lockstep_julia/README.md` first.
2) Then read `Tools/px4_lockstep_julia/docs/README.md` and any linked docs relevant to the task.
3) Inspect the git diff, identify the changed files and closely related files.
4) Read the relevant docs and then read the full code of those files to grasp context before editing.
